package aqua.blatt1.common.msgtypes;

import java.io.Serializable;

public class LocationUpdate implements Serializable {
    /*
    • Schwimmt ein Fisch in einen neuen Tank, ist zu unterscheiden, ob der Fisch (a)
urspr¨unglich in diesem Aquarium beheimatet ist oder (b) nicht. Im Fall (a)
muss der eigene homeAgent entsprechend aktualisiert werden. Im Fall (b) muss
der Klient das Heimataquarium des Fisches informieren. Dazu sendet er einen
NameResolutionRequest an den Broker. Die dazu ben¨otigte Tank-ID bekommt
der Klient vom Fisch selbst (FishModel.getTankId). Als Request-ID verwenden Sie die Fisch-ID. Sobald – zu einem sp¨ateren Zeitpunkt – die Antwort eintrifft, wird das Heimataquarium mit Hilfe des neu zu erstellenden Nachrich
     */
}
